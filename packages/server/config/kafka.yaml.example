groupID: automation_local
transLogV1:
  brokers: localhost:9092
  topic: ZPReportTransLogQC
refundLog:
  brokers: localhost:9092
  topic: ZPReportTransLog
  # ssl and sasl are optional per-topic — omit both for a plaintext, unauthenticated connection.
  # ssl: true
  # sasl:
  #   mechanism: plain
  #   username: qa-user
  #   password: qa-pass
paymentAuth:
  brokers: localhost:9092
  topic: payment_authentication_auth_session_status_qc
disburseLog:
  brokers: localhost:9092
  topic: td-transfer-disbursement-order-status-qc
